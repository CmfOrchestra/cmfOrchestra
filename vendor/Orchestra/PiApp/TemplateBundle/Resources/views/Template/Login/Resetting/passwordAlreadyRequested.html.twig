{% extends "PiAppTemplateBundle:Template\\Login:layout.html.twig" %}
{% block content %}
<div id="form-connexion-request-response">
	<section class="forgotten-password">
		<header class="tt-grey resetM">
			<h3 class="uppercase bold tt-10">Mot de passe oublié ?</h3>
		</header>
		<p id="msg_exist">{{ 'resetting.password_already_requested'|trans({}, 'FOSUserBundle') }}</p>
                <br/><br/>
                {% if app.request.query.get('NoLayout') == "" %}
                    <a href="{{ path('admin_homepage') }}" class="button" style="float:right;"> Retour</a>
                {% endif %}
	</section>
</div>
{% endblock content %}
