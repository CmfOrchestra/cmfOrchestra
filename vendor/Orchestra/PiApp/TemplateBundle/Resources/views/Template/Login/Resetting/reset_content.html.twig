{#<div class="third-col-tpl fleft">
	<section class="bloc-connexion">
		<div class="wrap">
			<div class="content">
				{% form_theme form theme %}
				<form action="{{ path(route, {'token': token}) }}" {{ form_enctype(form) }} method="POST" id="form-connexion-reset">
					<div class="title-border-simple cf">
						<h3 class="tt-3 tt-clr bold fleft">Création/Changement de mot-de-passe</h3>
					</div>			
					<fieldset class="resetBorder">	
						<div class="row">
				    	{{ form_widget(form) }}
				    	</div>
				    	<div class="button-ct">
				        	<input type="submit"  class=" nextStep" value="{{ 'pi.grid.action.validate'|trans }}" />
				    	</div>
				    </fieldset>
				</form>
			</div>
		</div>
	</section>		
</div>
#}

<div class="leftCol largeCol fleft cf"  style="margin-top: 20px;">
    <h4 class="tt2">{{ 'resetting.request.submit'|trans([], 'FOSUserBundle') }}</h4>
    <div class="intro">
        <p>Vous avez oublié votre mot de passe.</p>
        <p>Afin de pouvoir accéder à votre compte, veuillez saisir votre nouveau mot de passe et confirmer.</p>
    </div>
    <form action="{{ path(route, {'token': token}) }}" {{ form_enctype(form) }} method="POST" id="form-connexion-reset" novalidate>
        <p class="errorMsg long">
            {{ form_errors(form) }}
            {{ form_widget(form._token) }}
        </p>
        <div class="info">
            <div class="row cf">
                <label>Nouveau Mot de Passe<span>*</span> :</label>
                {{ form_widget (form.plainPassword.children['first'], { 'attr': { 'class': 'input required','type':'password' }}) }}
            </div>
            <div class="row cf">
                <label>Confirmation du nouveau Mot de Passe<span>*</span> :</label>
                {{ form_widget (form.plainPassword.children['second'], { 'attr': { 'class': 'input required','type':'password' }}) }}
            </div>
            <div class="row cf">
                <input type="submit" value="{{ 'resetting.reset.submit'|trans([], 'FOSUserBundle') }}"/>
            </div>
        </div>
    </form>
</div>