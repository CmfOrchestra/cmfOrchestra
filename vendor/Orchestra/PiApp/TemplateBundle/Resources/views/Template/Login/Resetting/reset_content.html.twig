{#<div class="third-col-tpl fleft">
	<section class="bloc-connexion">
		<div class="wrap">
			<div class="content">
				{% form_theme form theme %}
				<form action="{{ path(route, {'token': token}) }}" {{ form_enctype(form) }} method="POST" id="form-connexion-reset">
					<div class="title-border-simple cf">
						<h3 class="tt-3 tt-clr bold fleft">Création/Changement de mot-de-passe</h3>
					</div>			
					<fieldset class="resetBorder">	
						<div class="row">
				    	{{ form_widget(form) }}
				    	</div>
				    	<div class="button-ct">
				        	<input type="submit"  class=" nextStep" value="{{ 'pi.grid.action.validate'|trans }}" />
				    	</div>
				    </fieldset>
				</form>
			</div>
		</div>
	</section>		
</div>
#}
<section class="bloc-connexion">
	<div class="wrap content">
		<header class="resetM">
			<div class="title-border-simple cf">
				<h3 class="tt-5 tt-clr fleft">Création/Changement de mot-de-passe</h3>
			</div>
		</header>
		<form action="{{ path(route, {'token': token}) }}" {{ form_enctype(form) }} method="POST" id="form-connexion-reset">
					<div class="row">
						{{ form_errors(form) }}
    					{{ form_widget(form._token) }}
					</div>
					<div class="row">
						{{ form_widget(form.plainPassword) }}
					</div>
					<div class="txtcenter">
						<input type="submit" class="txtcenter nextStep" value="{{ 'pi.grid.action.validate'|trans }}">
					</div>
		</form>
	</div>
</section>