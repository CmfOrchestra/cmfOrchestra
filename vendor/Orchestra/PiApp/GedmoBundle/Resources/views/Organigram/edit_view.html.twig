	<!-- Begin tag markup -->
	<div id="tabs">
		<ul>
			<li><a href="#tabs-1">{{ 'pi.organigram.translation.title'|trans }}</a></li>
		</ul>

		{% form_theme edit_form getParameter('pi_app_admin.layout.template.form') %}
		<form class="myform" action="{{ path('admin_gedmo_organigram_update', { 'id': entity.id, 'NoLayout': NoLayout}) }}" method="post" {{ form_enctype(edit_form) }} novalidate>
		    <div id="tabs-1">
		    	{{ form_widget(edit_form) }}
		    </div>
		    <p>
		        <!-- <input id="add_tab" type="button" value="{{ 'pi.grid.action.add-tab'|trans }}" /> -->
		        <button type="submit">{{ 'pi.grid.action.save'|trans }}</button>
		    </p>		    
		</form>
		
	</div>
	<!-- End tag markup -->	
	
	{% set options = {'prototype-name': ['translations'], 'prototype-tab-title': 'Translate'} %}
	{{ renderJquery('FORM', 'prototype-bytab', options )|raw }}		
		
	<ul class="ui-widget-header">
		<li>
	        <form action="{{ path('admin_gedmo_organigram', {'NoLayout':NoLayout}) }}" method="post">
	            <button type="submit">{{ 'pi.grid.action.back-to-the-list'|trans }}</button>
	        </form>	        
		</li>
	    <li>
	        <form action="{{ path('admin_gedmo_organigram_delete', { 'id': entity.id, 'NoLayout':NoLayout }) }}" method="post">
	            {{ form_widget(delete_form) }}
	            <button type="submit">{{ 'pi.grid.action.delete'|trans }}</button>
	        </form>
	    </li>
	</ul>
	
	{% if entity.media.image is defined %}
	{{ picture_form(entity.media.image, 'piapp_gedmobundle_organigramtype_media_image_binaryContent', 'reference')|raw }}
	{% endif %}
	
	<script type="text/javascript">
	//<![CDATA[
			//j_prototype_bytabs.ftc_dialog_form("left_picture", "Left picture", ".myform", 400, 366, "center");
			j_prototype_bytabs.ftc_accordion_form("image_collection", "{{ 'pi.organigram.form.group.pictures'|trans }}", ".myform");								
	//]]>
	</script>