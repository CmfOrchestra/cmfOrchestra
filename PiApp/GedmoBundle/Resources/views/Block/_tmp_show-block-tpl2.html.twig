<section class="bloc-generic tpl-2 cf">
	<div class="wrap">
		<figure>
			{% if entity.media.image.id is defined %}
			<img src="{{ media_url(entity.media.image.id, 'reference', true, entity.media.getUpdatedAt(), 'block_') }}" title="" alt="" />
			{% endif %}
			{% if entity.media1.image.id is defined %}
			<img src="{{ media_url(entity.media1.image.id, 'reference', true, entity.media1.getUpdatedAt(), 'block_') }}" title="" alt="" />
			{% endif %}
		</figure>
		{{ entity.translate(locale).getContent()|raw }}
	</div>
</section>